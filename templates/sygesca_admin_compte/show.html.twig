{% extends 'base.html.twig' %}

{% block title %}Compte{% endblock %}

{% block body %}
    <h1>Compte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compte.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ compte.statut ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sygesca_admin_compte_index') }}">back to list</a>

    <a href="{{ path('sygesca_admin_compte_edit', {'id': compte.id}) }}">edit</a>

    {{ include('sygesca_admin_compte/_delete_form.html.twig') }}
{% endblock %}
