<form method="post" action="{{ path('sygesca_objectfif_delete', {'id': objectif.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cet objectif?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ objectif.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
